version: 2
updates:
  - package-ecosystem: "npm"
    directory: "/"
    schedule:
      interval: "weekly"
    open-pull-requests-limit: 5

  # Rust/Cargo disabled — glib version conflicts cause CI failures
  # - package-ecosystem: "cargo"
  #   directory: "/src-tauri"
  #   schedule:
  #     interval: "weekly"
